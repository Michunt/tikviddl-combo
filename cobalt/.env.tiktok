# TikTok-only Downloader API Configuration

# Required: API URL
API_URL=http://localhost:9000/

# Server Configuration
API_PORT=9000
API_LISTEN_ADDRESS=0.0.0.0

# CORS Configuration
CORS_WILDCARD=1
# If you want to restrict CORS, uncomment and set your domain:
# CORS_URL=https://yourdomain.com

# Rate Limiting
RATELIMIT_WINDOW=60
RATELIMIT_MAX=20
SESSION_RATELIMIT_WINDOW=60
SESSION_RATELIMIT=10
TUNNEL_RATELIMIT_WINDOW=60
TUNNEL_RATELIMIT=40

# Content Limits
DURATION_LIMIT=10800
TUNNEL_LIFESPAN=90

# Disable all services except TikTok
DISABLED_SERVICES=bilibili,bluesky,dailymotion,instagram,facebook,loom,ok,pinterest,reddit,rutube,snapchat,soundcloud,streamable,tumblr,twitch,twitter,vimeo,vk,xiaohongshu,youtube

# Optional: Security features (uncomment if needed)
# TURNSTILE_SITEKEY=your_turnstile_sitekey
# TURNSTILE_SECRET=your_turnstile_secret
# JWT_SECRET=your_jwt_secret
# JWT_EXPIRY=120

# Optional: API Keys (uncomment if needed)
# API_KEY_URL=file://keys.json
# API_AUTH_REQUIRED=1

# Optional: Processing
# PROCESSING_PRIORITY=10 (Windows doesn't support nice)
FORCE_LOCAL_PROCESSING=never

# Optional: Networking
# API_EXTERNAL_PROXY=http://proxy.example.com:8080
# FREEBIND_CIDR=2001:db8::/32

# Optional: Redis (for clustering)
# API_REDIS_URL=redis://localhost:6379
# API_INSTANCE_COUNT=1
